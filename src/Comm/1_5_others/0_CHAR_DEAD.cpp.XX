#include "Sharun.hpp"

void CHAR_DEAD_f(player_t *player, player_t *player2)
{
/*	packet_l->write_2(player, OpCodes_GetOpCode(player, CHAR_DEAD));
	packet_l->write_4(player, player2->Id);
	packet_l->write_4(player, Sharun->Settings.PLAYER_SUBID);
	for (int i=0; i<3; i++)
		packet_l->write_F(player, player2->XYZ[i]);
	packet_l->write_2(player, player2->Stats.HP ? 1 : 0);
	Player_UpdateHeaderS(player);*/
}
